# Public environment variables for bot (non-sensitive)
# These can be safely included in Docker images

PORT=3000
MINI_APP_URL=http://localhost:3001
SIDESHIFT_CLIENT_IP=127.0.0.1

# Non-sensitive configuration
NODE_ENV=production