# ── Secrets (NEVER commit) ─────────────────────────────────
.env

# ── Hardhat / Solidity build artefacts ────────────────────
artifacts/
cache/
typechain-types/
coverage/
coverage.json

# ── Node modules ──────────────────────────────────────────
node_modules/

# ── Deployment receipts (generated by hardhat-toolbox) ────
deployments/

# ── Editor / OS ───────────────────────────────────────────
.DS_Store
Thumbs.db
*.log
